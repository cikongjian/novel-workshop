# 阶段 1 功能测试指南

## 测试环境准备

1. 启动后端服务：
```bash
npm run dev
```

2. 启动前端服务：
```bash
cd web
npm run dev
```

## 功能 1: AI 标记守卫测试

### 测试步骤

1. 打开章节工作区，选择一个已有章节
2. 点击"修订章节"按钮
3. 在修订对话框中：
   - 启用"AI 标记守卫"开关
   - 设置阈值为 5（默认值）
   - 输入修订反馈："增加更多细节描写"
4. 点击"开始修订"

### 预期结果

- 修订完成后，显示 AI 标记分数变化
- 如果分数下降超过 5 分，修订被拒绝，显示警告消息
- 如果分数下降未超过阈值，修订应用，显示信息提示
- 如果分数提升，显示成功消息

### 验证点

- [ ] AI 标记守卫开关正常工作
- [ ] 阈值设置生效
- [ ] 分数对比正确显示
- [ ] 拒绝逻辑正确执行

---

## 功能 2: Spot-Fix 修订模式测试

### 测试步骤

1. 打开章节工作区，选择一个已有章节
2. 点击"修订章节"按钮
3. 在修订对话框中：
   - 选择"定点修复（只改问题句）"模式
   - 输入具体的修订反馈："第3段的对话不够自然，请改写"
4. 点击"开始修订"

### 预期结果

- Editor Agent 只修改第3段的对话
- 其余段落保持不变
- 编辑笔记中标注修改位置

### 对比测试

1. 使用相同的反馈，选择"全文润色"模式
2. 观察修订结果的差异

### 验证点

- [ ] 模式选择器正常工作
- [ ] Spot-Fix 模式只修改指定部分
- [ ] 全文润色模式修改整章内容
- [ ] 帮助文本清晰易懂

---

## 功能 3: 支线进度板测试

### 前置条件

需要一个包含多条情节线的小说项目。如果没有，可以：

1. 创建新小说
2. 在大纲编辑器中添加 3-5 条情节线
3. 设置不同的状态（planted, developing, resolved）
4. 生成至少 10 章内容

### 测试步骤

1. 打开小说布局页面
2. 在侧边栏或工具面板中找到"支线进度板"入口
3. 点击打开支线进度板

### 预期结果

- 显示所有情节线的列表
- 每条线显示：名称、状态、植入章节、最后推进、停滞章数
- 停滞超过 5 章的支线高亮显示（红色警告图标）
- 停滞支线排在列表前面

### 验证点

- [ ] 进度板正确加载
- [ ] 停滞检测逻辑正确（>5章未推进）
- [ ] 状态标签颜色正确
- [ ] 排序逻辑正确（停滞优先）
- [ ] 更新时间显示正确

---

## API 测试

### 测试 AI 标记守卫 API

```bash
curl -X POST http://localhost:3001/api/generate/revise \
  -H "Content-Type: application/json" \
  -d '{
    "novelId": "your-novel-id",
    "chapterNumber": 1,
    "feedback": "增加更多细节描写",
    "enableAiMarkerGuard": true,
    "aiMarkerGuardThreshold": 5,
    "mode": "rewrite"
  }'
```

### 测试 Spot-Fix 模式 API

```bash
curl -X POST http://localhost:3001/api/generate/revise \
  -H "Content-Type: application/json" \
  -d '{
    "novelId": "your-novel-id",
    "chapterNumber": 1,
    "feedback": "第3段对话不自然",
    "mode": "spot-fix"
  }'
```

### 测试支线进度板 API

```bash
curl http://localhost:3001/api/novels/your-novel-id/subplot-board
```

---

## 命令行测试

### 测试支线进度板构建器

```bash
node scripts/test-subplot-board.js your-novel-id
```

预期输出：
```
正在构建小说 xxx 的支线进度板...

=== 支线进度板 ===
小说 ID: xxx
当前章节: 第 10 章
支线总数: 3
更新时间: 2026-03-15 14:30:00

⚠️  停滞支线: 1 条

=== 支线详情 ===

⚠️ 主角成长线
  状态: developing
  植入章节: 第 1 章
  最后推进: 第 3 章
  停滞章数: 7
  下一步: 推向高潮

反派阴谋线
  状态: developing
  植入章节: 第 2 章
  最后推进: 第 9 章
  停滞章数: 1
  下一步: 推向高潮

✅ 支线进度板构建成功
```

---

## 回归测试

确保新功能不影响现有功能：

- [ ] 章节生成正常工作
- [ ] 普通修订（不启用守卫）正常工作
- [ ] 章节重写正常工作
- [ ] 批量生成正常工作
- [ ] 其他管线功能正常工作

---

## 已知问题

1. **支线进度板限制**: 当前 `lastAdvancedInChapter` 使用简化逻辑，未扫描每章的 `plotThreadsAdvanced` 字段
2. **角色名称**: 相关角色显示 ID 前缀而非真实名称
3. **构建错误**: 项目中存在其他文件的类型错误，但不影响新功能

---

## 问题反馈

如果发现问题，请记录：
- 操作步骤
- 预期结果
- 实际结果
- 错误消息（如有）
- 浏览器控制台日志
- 后端日志
